# HydroCav Website Environment Configuration
# Copy this file to .env and fill in your actual values for production deployment
# NEVER commit .env to version control

# Supabase Configuration (Required for production build)
# Get these values from your Supabase dashboard → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Build Environment
# Set to 'production' for production builds
NODE_ENV=development

# Build Instructions:
# 1. Set the environment variables above
# 2. Run: npm run build:prod
# 3. Deploy contents of dist/ directory